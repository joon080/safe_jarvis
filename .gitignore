# Mark-XXXIX 원본 (참고용, 업스트림 코드 — 통째로 커밋하지 않음)
/mark_base/

# 파이프라인 실행 로그
.project-ai/_runs/

# 개인 개발 노트 (로컬 경로·내부 설계 메모 포함)
CLAUDE.md

# 진단/테스트 스크립트 (로컬 임시 파일)
mike_test.py

# 비밀/설정
config/api_keys.json
.env
.env.*
secrets.json
credentials.json

# 장기 기억 (이름·관계 등 개인정보 저장 파일 — 절대 커밋 금지)
memory/long_term.json

# Python
__pycache__/
*.pyc
.venv/
venv/
