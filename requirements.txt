# safe_jarvis 파트 B — UI + 음성 + AI 비서
# 설치: py -3.11 -m pip install -r requirements.txt

# Core AI (Gemini)
google-generativeai>=0.8.0
google-genai>=0.8.0

# GUI
PyQt6>=6.6.0

# 오디오 (녹음 + 박수감지)
sounddevice>=0.4.6
numpy>=1.24

# STT (Google Web Speech API, 무료)
SpeechRecognition>=3.10

# Wake word "Hey Jarvis" (openWakeWord — 완전 오프라인, 키/가입 불필요)
openwakeword>=0.6
onnxruntime>=1.16

# TTS (EdgeTTS = 온라인, 한국어/영어 고품질 / pyttsx3 = 오프라인 폴백)
edge-tts>=7.0
pyttsx3>=2.90

# 시스템 모니터링 (UI 좌측 패널)
psutil>=5.9

# 이미지 처리 (HUD 얼굴 이미지)
Pillow>=10.0

# 웹 검색 / 날씨
requests>=2.31
beautifulsoup4>=4.12
duckduckgo-search>=6.0

# 화면 캡처 (screen_process)
mss>=9.0
opencv-python>=4.9

# YouTube 요약 (선택)
youtube-transcript-api>=0.6
